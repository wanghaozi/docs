---
title: "更新日志 — 2023"
description: >-
  Warp 2023 年发布说明。更新每周发布，通常在周四。
---

在我们的 [GitHub 面板](https://github.com/warpdotdev/Warp/issues/new/choose)上提交错误和功能请求！

### 2023.02.08 (v0.2024.02.13.08.02)

**Bug 修复**

* 修复输入的命令有时与 rprompt（右侧提示符）重叠的问题

### 2023.02.01 (v0.2024.01.30.16.52)

**改进**

* 改进了粘贴认证令牌以完成登录流程的用户体验
* 子版本（svn）信息现在可在 Warp 的提示符中显示

### 2023.01.18 (v0.2024.01.16.16.31)

**新功能**

* Linux 版 Warp（私有测试版）：系统字体现在可按预期加载

**Bug 修复**

* Linux 版 Warp（私有测试版）：`ALT-TAB` 不再错误地在输入编辑器中插入 4 个空格

### 2023.01.11 (v0.2024.01.09.08.02)

**新功能**

* Warp Drive 中共享工作流的新工作流元数据！悬停在任何工作流上，了解工作流最近执行的时间、谁最后编辑了它以及最后编辑的时间。

### 2023.12.21 (v0.2024.01.02.08.02)

**改进**

* 悬停在背景块上时显示的工具带现在有实色背景

**Bug 修复**

* Markdown 查看器现在尊重有序列表的起始编号
* 包含波浪号（`~`）字符的路径补全现在按预期工作
* 修复了 Warp 在保存对 Warp Drive 的更改之前可能退出的问题
* 修复了使用"插入到输入"上下文菜单操作时 Warp 挂起的问题

### 2023.12.14 (v0.2023.12.12.08.02)

**新功能**

* 使用 Vim 键位进行编辑现已退出测试版并全面可用！Warp 将检测 shell 设置中的 vi 模式并建议 Vim 键位。

**改进**

* 你现在可以在 Markdown 查看器中使用 `CMD-F` 搜索文本

**Bug 修复**

* 块悬停按钮在与提示符重叠时现在有实色背景
* 块筛选编辑器现在有清除按钮
* `J` 和 `K`（Vim 模式）可用于在多行命令中导航
* 修复了 macOS 全屏模式下标签栏的左对齐问题
* 修复了筛选块时的三击选择（选择一行）问题
* 修复了使用查找栏时可能发生的崩溃
* 修复了获取辅助功能内容时可能发生的崩溃
* 修复了在 zsh 会话中"R"被错误插入输入的问题

### 2023.12.07 (v0.2023.12.05.08.02)

**改进**

* Markdown 文件链接现在可以配置为使用默认外部编辑器或 Warp 内置的 Markdown 查看器打开
* Warp Drive 文件夹现在在应用重启后保持打开/关闭状态

**Bug 修复**

* 粘贴终端内容并运行命令后，输入编辑器现在正确重新获得焦点
* 修复了使用 Fish 和 Vim 模式时缺少工具带按钮的问题

### 2023.11.30 (v0.2023.11.28.08.02)

**改进**

* Warp 的自定义提示符构建器现在包含 Kubernetes 上下文的上下文标签
* 改进了 kubectl 的补全，包括资源、全局选项和命名空间的建议

**Bug 修复**

* 修复了工作流编辑器中参数编辑器溢出的 UI bug
* 使用命令面板打开启动配置时，搜索栏按预期聚焦

### 2023.11.16 (v0.2023.11.14.08.02)

**Bug 修复**

* 显示工作流元数据的信息块现在随 Warp 窗口调整大小
* 滚动速度现在在 Warp 中标准化

### 2023.11.09 (v0.2023.11.07.08.02)

**新功能**

* 新的 Markdown 查看器：你现在可以在 Warp 中打开 .md 文件并运行这些文件中的 shell 命令
* 块筛选：你现在可以筛选块输出（SHIFT-OPT-F）以快速根据查询找到匹配的行

**改进**

* 从块的工具带区域（右上角按钮）移除了工作流按钮。仍然可通过右键菜单（参见"保存为工作流"）和其默认键位 CMD-S 访问
* 改进了 Warp Drive 团队和状态同步的性能

### 2023.11.02 (v0.2023.10.31.08.03)

**改进**

* 你现在可以通过电子邮件地址邀请新团队成员加入共享的 Warp Drive，并撤销邀请

### 2023.10.23 (v0.2023.10.17.08.03)

**改进**

* 标签栏中出现指示器，显示当前窗格已最大化（全屏图标）以及命令以错误退出时
* Warp 提示符中的 Git 上下文标签在分离状态下现在显示提交哈希而不是"HEAD"
* 邀请团队成员加入 Warp Drive 时，更容易添加和移除允许列表中的域名
* 添加了复制工作流命令（到剪贴板）的菜单选项

### 2023.10.19 (v0.2023.10.17.08.03)

**改进**

* 标签栏中出现指示器，显示当前窗格已最大化（全屏图标）以及命令以错误退出时
* Warp 提示符中的 Git 上下文标签在分离状态下现在显示提交哈希而不是"HEAD"
* 邀请团队成员加入 Warp Drive 时，更容易添加和移除允许列表中的域名
* 添加了复制工作流命令（到剪贴板）的菜单选项

### 2023.10.12 (v0.2023.10.10.08.06)

**改进**

* Warp 现在可以支持 macOS 的代理设置
* 你现在可以切换是否使用集成 GPU 渲染 Warp（适用于双 GPU Mac）
* Warp 现在转义从 Finder 加载的可执行文件的文件路径

**Bug 修复**

* 修复了部分 macOS Sonoma 用户启动时的崩溃
* 工作流信息框现在在编辑时刷新

### 2023.10.05 (v0.2023.10.03.08.03)

**新功能**

* 现在你可以在 Warp 中使用 Vim 键位编辑命令行文本。导航至设置 > 功能 > 编辑器并启用"使用 Vim 键位编辑命令"。此功能目前处于测试阶段，今天即可试用。

**改进**

* 管理员现在可以控制团队成员是否可以复制和分享团队邀请链接。管理员也可以在需要时重置 URL 令牌。
* 你现在可以使用提示符编辑器中的上下文标签向 Warp 提示符添加 24 小时制时间戳。
* 团队版 Warp AI 和 Warp Drive 的免费预览已延长。[了解更多](https://www.warp.dev/blog/free-preview-extended)

### 2023.09.28 (v0.2023.09.26.08.09)

**新功能**

* 如果你安装了 Cursor，现在可以在设置 > 功能 > 通用中将其设置为默认代码编辑器。

**改进**

* 增强了用户无障碍体验，添加了标签栏按钮作为命令面板的新入口。
* 改进了用户引导，在尝试运行工作流但另一条命令正在执行时显示警告。

**Bug 修复**

* 解决了绑定到某些键位时自动建议未被插入的问题。
* 修复了影响非英文键盘输入法编辑器功能的问题，该问题导致打开新窗口后定位不正确并阻止文本输入。

### 2023.09.14 (v0.2023.09.19.08.04)

**新功能**

* 你现在可以编辑向上和向下滚动一行的键位。

**改进**

* 使用分屏窗格时，输入编辑器在非活动窗格中保持可见。

**Bug 修复**

* 解决了已添加书签的块上的书签图标在悬停前不显示的回归问题。
* 修复了 `TAB` 键在某些情况下无法在工作流编辑器中循环切换字段的问题。
* 恢复了在没有打开窗口时"新标签页"键位的功能。

### 2023.09.07 (v0.2023.09.06.18.09)

**改进**

* 新建标签页的键盘快捷键（默认为 `CMD-T`）现在可以重新映射。
* Warp Drive 现在在同步时显示加载指示器。

**Bug 修复**

* 当输入编辑器固定在顶部时，命令时间戳工具提示不再被隐藏。

### 2023.08.31 (v0.2023.08.29.08.04)

**改进**

* 你现在可以从 Warp UI 中删除自定义主题。
* 你现在可以从命令面板滚动到选中块的顶部或底部。

**Bug 修复**

* 修复了 git 进程消耗 CPU 的问题（[3563](https://github.com/warpdotdev/warp/issues/3563)）。
* 修复了 Zsh 中 `set sh_word_split` 可能破坏 Warp 引导的 bug。

### 2023.08.24 (v0.2023.08.22.08.03)

**新功能**

* 秘密涂抹——Warp 现在可以自动涂抹终端输出中的秘密和敏感信息，包括密码、IP 地址、API 密钥和 PII。从命令面板或设置 > 隐私 > 秘密涂抹启用秘密涂抹。

**改进**

* 与 `META` 结合使用的特殊键（例如 `META-DELETE`）现在应在备用屏幕中正常工作。
* 当在设置 > 外观 > 文本 > 行高中更新自定义高度时，输入编辑器中文本的行高现在应实际更改。
* Fish 中的别名缩写不应再在输入编辑器中显示红色错误下划线。
* 在紧凑模式下减少了输入编辑器中的底部内边距。

### 2023.08.17 (v0.2023.08.15.08.03)

**新功能**

* Warp 现在为历史记录中的每条命令显示更丰富的元数据，包括退出码、工作目录、git 分支以及命令是否为工作流的一部分。
* Warp 的原生提示符现在可以通过拖放上下文标签直接在应用内自定义（设置 > 外观 > 提示符）。

**改进**

* Warp 现在支持 xterm 的焦点报告转义码（序列）。
* 命令面板现在除了工作流的名称和描述外，还可以使用其 Warp Drive 文件夹名称搜索工作流。
* 从起始图像自动生成自定义主题现在即使缺少 `~/.warp/themes` 目录也能工作。
* "新建工作流"弹窗现在支持更长命令的更多文本。

### 2023.08.10 (v0.2023.08.08.08.04)

**新功能**

* 基于背景图像自动创建新主题！点击主题选择器中的 `+` 按钮（设置 -> 外观 -> 当前主题）或在命令面板中搜索"打开主题选择器"。

**改进**

* Warp Drive 中的工作流和文件夹现在可以按字母顺序和最后更新时间排序。
* 多个 JetBrains IDE 现在作为外部编辑器受支持（例如 WebStorm、PhpStorm、GoLand）。
* 命令面板现在显示工作流所在的文件夹（面包屑导航）。
* 像 `...` 和 `....` 这样的别名不再错误地带有错误下划线。

### 2023.08.03 (v0.2023.08.01.08.05)

**新功能**

* 使用 `SHIFT-CMD-T` 重新打开关闭的标签页，最多一分钟；可在设置 > 功能 > 启用重新打开已关闭的会话中配置或禁用此功能。
* 使用 Warp AI 自动为 Warp Drive 中的工作流生成描述。

**改进**

* Warp Drive 中的嵌套文件夹现在可以一次性折叠。
* 修复了 fish 缩写扩展包含注释的问题。
* 修复了 fish 历史记录变得不可访问的回归问题。

### 2023.07.27 (v0.2023.07.25.08.03)

**改进**

* 修复了 $PATH 可能在 Bash 子 shell 中被覆盖的问题。
* 修复了使用命名标志（例如 `ls --color=auto`）时文件路径补全失效的问题。
* 修复了 Warp Drive 对象可能卡在同步状态的问题。
* 向下箭头 `DOWN` 现在正确地在 Warp AI 的文本编辑器中移动光标。

### 2023.07.20 (v0.2023.07.18.08.03)

**新功能**

* 现在可以配置 `TAB` 是接受自动建议还是打开补全菜单；通过设置 > 功能 > 编辑器在二者之间切换。
* 通过改进公共前缀检测和支持大小写敏感，改进了补全行为。
* 现在可以原生绘制某些 Unicode 块元素字符，而不使用字体字形——改善了对齐并减少了模糊。
* Warp 的资源中心现在显示新功能和改进。

**改进**

* 将最大模糊半径从 18 增加到 64。

### 2023.07.13 (v0.2023.07.11.08.03)

**新功能**

* 同步失败的 Warp Drive 项目现在可以重试。
* Warp Drive 中的工作流现在可以通过工作流执行弹窗进行编辑。

**改进**

* 修复了 git 信息有时可能从提示符中消失的 bug。
* 调整了 Warp 中的一些颜色——将渐变替换为实色。

### 2023.07.06 (v0.2023.07.04.08.03)

**新功能**

* 新的 AI 命令搜索体验，允许你将自然语言翻译为 shell 命令，并可直接与工作流集成！在输入中键入 '#' 试试！

**改进**

* 修复了 Warp 无法识别某些单字符命令和别名的 bug。
* 修复了命令输出有时在完成后被截断的 bug（最明显的是在 Gradle 中）。
* 修复了远程 Bash 会话可能出现两个提示符的 bug。

### 2023.06.29 (v0.2023.06.27.19.34)

**新功能**

* 格式为 Warp://launch/\<launch\_configuration\_name> 的应用链接可直接打开启动配置。
* 添加了新设置，用于创建具有特定行数和列数尺寸的新窗口。

**改进**

* 修复了同一字符上多个 ANSI 样式的渲染。这修复了 Vim 和 emacs 中常见的渲染问题。
* 修复了在应打开补全菜单时，制表符（缩进）有时被插入输入编辑器的问题。
* 为"新建标签页"按钮添加了工具提示。
* "启动配置"子菜单（在 Mac 文件菜单下）现在在添加和移除启动配置时动态更新。
* 查找栏能够匹配双宽 Unicode 字符，包括 CJK 和表情符号。
* 修复了在工作流编辑器中粘贴命令时可能发生的崩溃。

### 2023.06.20 (v0.2023.06.20.08.04)

**新功能**

* 你现在可以将 Powerlevel10k (P10K) 提示符的强大功能带到 Warp 中！为获得最佳效果，你需要最新版本的 P10K；请参阅其 GitHub 页面了解升级说明。
* Zsh 和 fish 现在支持右侧提示符！
* Warp AI 命令现在可以作为工作流执行。

**Bug 修复**

* 点击非活动的 Warp 窗口现在正确聚焦底层窗格。

### 2023.06.08 (v0.2023.06.13.08.03)

**新功能**

* 为我们升级的推荐系统添加了设置页面——我们添加了新的赠品选项。
* 右键单击高亮的文件路径可打开上下文菜单，现在支持在 Finder 中显示文件。
* 命令面板现在可以搜索 Warp 会话、操作和启动配置。

**Bug 修复**

* 补全菜单现在支持 fish 缩写。
* 修复了某些别名在展开后不正确的问题。
* 修复了命令搜索忽略搜索查询前后额外空白的问题。
* 恢复的背景块不再创建空白历史条目。
* 修复了启用"输入时打开补全"设置有时可能破坏路径补全的问题。
* 修复了当 $PATH 处于错误状态时 Zsh 可能无法引导的问题。
* 修复了 Warp 的引导逻辑可能泄漏到 Zsh 历史中的问题。
* 修复了超链接在列表中或跨越多行时正确加下划线的问题。

### 2023.06.01 (v0.2023.05.30.08.03)

**新功能**

* 右键单击新建标签页（`+`）按钮可打开上下文菜单以选择已保存的启动配置。
* 在命令面板中使用 Page Up（`PG-UP`）和 Page Down（`PG-DOWN`）进行更快的导航。
* 添加了对 Zed 作为默认代码编辑器的支持。
* 推荐计数已更新，仅包括已完成引导（实际尝试使用）Warp 的推荐用户。

**Bug 修复**

* Warp 的热键窗口（Quake 模式）现在正确保持其大小。
* 修复了调整 Warp 大小时命令输出暂时截断的问题。
* 修复了粘性命令标题覆盖分页器命令内容的问题。
* 修复了重命名时标签页显示过时文本的问题。
* 单击带有子菜单的 Mac 菜单栏项目不再错误关闭菜单。
* 当键盘快捷键窗格打开时，Warp 现在自动聚焦快捷键搜索栏（`CMD-/`）。
* 修复了 Warp 的原生提示符不再显示虚拟环境的回归问题。

### 2023.05.25 (v0.2023.05.23.08.05)

**Bug 修复**

* 改进了系统重启后已安装 Xcode 的用户的 shell 启动性能。
* 修复了从 zsh 对 pipenv shell 子 shell 进行 Warpify 的问题。
* 修复了在远程子 shell 中更新 git 状态提示指示器的问题。

### 2023.05.18 (v0.2023.05.18.01.08)

**新功能**

* Warp 现在支持 Zsh、Bash 和 fish 中的子 shell，为 Docker、GCP、Poetry 等提供更好的体验。在设置 > 子 shell 下配置你希望"Warpify"的命令。

**Bug 修复**

* 修复了使用以单个短横线开头的标志（例如 `-namespace`）时 Warp 补全的问题。
* 修复了同步输入中从备用屏幕切换时聚焦到错误终端会话的问题。
* 修复了命令历史建议可能导致同步输入不同步的问题。

### 2023.05.11 (v0.2023.05.09.08.03)

**新功能**

* Warp 现在将后台 shell 进程的输出发送到新的（独立的）块中——与用户生成的块分开。
* 在单个标签页或多个标签页中跨多个窗格同步（广播）输入（Mac 菜单 > 编辑 > 同步输入或在命令面板中的"同步"）。
* 添加了启用（默认禁用）可听终端铃声的选项（设置 > 功能 > 终端或在命令面板中搜索"启用/禁用可听终端铃声"）。
* 现在新窗口使用最近关闭的窗口的位置和大小打开（如果有）。
* Fish 别名现在在补全菜单中受支持。

**Bug 修复**

* 支持备用屏幕编辑器中的 `SHIFT-UP` 和 `SHIFT-DOWN`。
* 修复了启用滚动报告时备用屏幕滚动行为不正确的问题。
* `SHIFT-TAB` 现在（正确）发送 ANSI（后退制表符）转义序列（用于 Vim 和 NeoVim）。
* SSH 包装器现在也会加载你的 /etc/profile，并支持类似登录的提示和交互，如打印每日消息（MOTD）。

### 2023.05.04 (v0.2023.05.02.08.03)

**新功能**

* 在设置 > 账户 > 关于 Warp 中指示 Warp 何时下载更新。
* 支持 bash/zsh 别名的别名扩展。

### 2023.04.27 (v0.2023.04.25.08.05)

**新功能**

* 支持 Fish 缩写。
* 在输入编辑器中右键单击可打开上下文菜单，你可以在其中拆分窗格等。

**Bug 修复**

* 在工作流创建对话框中以空白字符开头的命令不再破坏其参数解析器。
* 修复了由于与 Warp 包装器命名冲突导致命令被别名为 `comm` 的 bug。
* `向左剪切单词`（`CTRL-W`）和 `向右剪切单词`（`OPT-D`）现在使用 shell 剪贴板而不是系统剪贴板。

### 2023.04.13 (v0.2023.04.11.08.03)

**新功能**

* 在输入编辑器中通过 `CTRL-OPT-LEFT` 和 `CTRL-OPT-RIGHT` 按子词导航。
* 即使在记录被清除后，也可以使用 `UP` 箭头查看之前的 Warp AI 问题。

**Bug 修复**

* 修复了在非默认 shell 下代理 SSH 中的 bug。
* 背景模糊现在也适用于通过从 Finder 拖放打开的窗口。
* 粘性命令标题不再截断分页器的文本。

### 2023.04.06 (v0.2023.04.04.08.03)

**新功能**

* 输入位置和终端输出方向现在可配置。你可以让输入从顶部开始，随着运行新命令向下移动（按 `CTRL-L`、`CMD-K` 或键入 `clear` 可清屏并重置位置）。或者你可以将输入固定在窗格顶部，让终端输出反向流动。设置在设置 > 外观 > 输入位置下可用。
* 添加了"跳转到当前悬停块底部"的按钮，以便轻松到达输出底部。可通过设置 > 外观 > 块下的设置进行配置。
* Warp AI 记录现在可以通过键盘导航（`UP` / `DOWN` 箭头）。
* 在备用屏幕中添加了右键上下文菜单（仍然遵守鼠标报告和 SGR_MOUSE）。
* Warp AI 的历史提示可以通过 `UP`（箭头）访问。
* Warp AI 中的 `CMD-ENTER` 现在将选中的命令输入到输入编辑器中。

**Bug 修复**

* 工作流现在可以在命令搜索中按描述搜索。
* 将"询问 Warp AI"键位合并为一个。
* 修复了如果启用了"左/右 Option 键作为 Meta"，"按单词移动光标"和"按单词向左/向右选择"不工作的问题。
* 现在可以在正在执行的命令中取消光标导航绑定。

### 2023.03.30 (v0.2023.03.28.08.03)

**新功能**

* 如果检测到已知不兼容的自定义提示符，将显示警告。
* REPL 和子 shell 中的光标导航键位，例如 ⌥←、⌥→、⌥⌫、⌘←、⌘→、⌘⌫、⌘fn⌫。

**Bug 修复**

* 修复了输入建议工具提示可能溢出可见窗口的问题。
* 修复了与 Warp AI 的键位冲突。
* 修复了本地路径包含分隔符（不在前缀中）时的补全和语法高亮问题。

### 2023.03.23 (v0.2023.03.21.08.02)

**新功能**

* 添加了 VSCode Insiders 作为支持的代码编辑器。
* 添加了 pnpm 的补全。

**Bug 修复**

* 修复了包含多个命令的 AI 命令结果都在同一行渲染的问题。
* 通用搜索的可配置宽度现在持久化（不会在新会话中重置为默认值）。
* "复制提示符"现在如果启用，正确遵守你的 PS1 提示符。
* 修复了 cargo 的自动命令修正。

### 2023.03.20 (v0.2023.03.14.08.03)

**新功能**

* 添加了配置 Warp 在启动终端会话时应使用哪个 shell 的支持。在设置 -> 功能 -> 会话下配置。
* 标签页现在可以通过鼠标双击重命名。

**Bug 修复**

* 启动配置模板现在支持在 `cwd` 字段中使用 `~`。
* 双击标题栏中的按钮/标签页不再调整整个窗口大小。
* 块列表中的上下文菜单现在更醒目，通过点击更容易关闭。
* 增加了小型搜索框的可点击区域。
* 现在可以为清除所有块注册键盘快捷键。
* 修复了由于使用 `LC_ALL` 环境变量导致的某些与语言环境相关的问题。
* Xterm 转义码 OSC 4（"更改颜色"）在 PS1 中出现时不再导致应用崩溃。
* 修复了在关闭通知横幅后调整窗口大小时发生的崩溃。
* 修复了如果你取消设置键盘快捷键侧面板的键位时发生的崩溃。
* 将 Warp AI 添加到资源中心。

### 2023.03.16 (v0.2023.03.07.08.02)

**新功能**

* 推出 Warp AI ⚡ 获取错误和输出的解释，请求复杂的流程和脚本方面的帮助，轻松执行建议的命令，一切尽在 Warp 中！

### 2023.03.09 (v0.2023.03.07.08.02)

**新功能**

* 添加了对清除某个操作的键位的支持 [2300](https://github.com/warpdotdev/warp/issues/2300)。
* 添加了使用系统级激活热键显示/隐藏 Warp 窗口的支持 [2585](https://github.com/warpdotdev/warp/issues/2585)。
* 改进了侧边栏菜单"Warp 基本操作"/"键盘快捷键"的滚动速度 [2673](https://github.com/warpdotdev/warp/issues/2673)。
* 用户现在可以设置自定义键位来打开补全菜单。
* 启用/禁用鼠标报告不再默认绑定到 CMD-R。
* 切换鼠标报告启用时会显示一个横幅。

**Bug 修复**

* 修复了 SSH 主机为 Arch Linux 且使用最新 bash 包时 SSH 包装器永久挂起的问题 [2636](https://github.com/warpdotdev/warp/issues/2636)。
* 修复了 Bash 命令最后 20 个字符中含有转义码时产生错误输出的问题。
* 修复了较新 macOS 版本上 bash 提示符扩展的 bug。

### 2023.02.28 (v0.2023.02.28.08.03)

**新功能**

* Warp 现在在 `git push` 时建议创建 GitHub PR 的 URL。
* 命令搜索和工作流菜单现在可水平调整大小。

**Bug 修复**

* 修复了 Warp 未正确自动弹起的 bug。
* 修复了粘贴到 nano 中时格式丢失的问题。
* 修复了退出 `info` 时 Warp 未检测到进程终止的问题。
* 修复了 bash 提示符扩展在 v4.4 或更早版本上不工作的 bug。
* 修复了账户菜单中未显示头像的 bug。
* 修复了语法高亮和错误下划线对多字节字符的处理。
* 修复了"检查更新"未反映当前状态的问题。

### 2023.02.23 (v0.2023.02.21.08.03)

**新功能**

* 支持配置新会话的初始工作目录。新标签页/窗口/分屏窗格可以有不同的配置，或者你可以为所有新会话设置一个值。

**Bug 修复**

* Warp 现在支持多行输入与多字节字符的语法高亮和错误下划线。
* 修复了 Warp 的"关于"部分中更新状态不正确的问题。
* 改进了打开多个窗口时的 GPU 内存消耗。

### 2023.02.16 (v0.2023.02.14.08.05)

**新功能**

* 改进了双击选择。双击文本现在智能选择文件路径、URL、电子邮件地址等模式 - [659](https://github.com/warpdotdev/warp/issues/659)
* 现在可以从 Finder 打开 Warp - [102](https://github.com/warpdotdev/warp/issues/102)

**Bug 修复**

* 退出备用屏幕后——使用查找搜索文本后——Warp 不再挂起。
* 块列表现在在从备用屏幕返回后滚动到正确位置。
* 点击滚动条上方不再（错误地）改变其滚动位置。
* 终端单元格尺寸现在在修改字体大小后立即更新。
* 超链接在 Warp 未聚焦时不再错误地悬停高亮。
* 输入法编辑器（非英文键盘）现在在备用屏幕和正在运行的块中正确定位。
* 当没有窗口打开时，从 Mac 菜单点击"新建标签页"将创建一个新窗口。

### 2023.02.09 (v0.2023.02.07.08.03)

**Bug 修复**

* Warp 现在设置 Mac 窗口标题；右键点击 dock 图标将显示活动标签页的名称。
* 修复了使用箭头键导航主题选择器时，如果没有主题匹配搜索，会导致崩溃的 bug。
* 点击超链接后，输入编辑器现在正确重新获得焦点。
* 包含 `SPACE` 键的自定义键位在关闭 Warp 后仍然保持。
* 输入法编辑器（非英文键盘）现在正确定位在输入编辑器中。

### 2023.01.26 (v0.2023.01.24.08.03)

**新功能**

* Warp 现在可以变暗非活动的终端窗格，导航至设置 > 外观 > 窗格 > 变暗非活动窗格。

**Bug 修复**

* 修复了使用 `CTRL-G` 选择多字节字符的多个匹配项时的崩溃。

### 2023.01.19 (v0.2023.01.17.08.03)

**新功能**

* 当前 Git 分支现在可以通过命令面板（`CMD-P`）复制。

**Bug 修复**

* 修复了某些键位操作被应用到错误终端窗格的问题。
* Warp 现在检查字体大小和行高的输入值，如果值太小或太大则忽略它们。
* "缺少更新权限"横幅现在可以关闭。
* 修复了关闭由启动配置创建的窗格时的罕见崩溃。

### 2023.01.12 (v0.2023.01.10.08.02)

**新功能**

* 支持通过设置 > 外观下的滑块设置窗口背景透明度和模糊半径。
* 改造的资源中心！点击右上角的 Warp 图标查看键盘快捷键并学习如何最好地使用 Warp。
* 退出弹窗：在会话正在运行时退出或关闭 Warp 会触发警告提示——也可让你查看哪些会话正在运行。
* 添加了禁用光标闪烁的开关。

**Bug 修复**

* 现在可以支持带有转义路径的补全。
* 现在可以支持路径以 \~ 开头的背景图像。
* 现在可以在使用多显示器时正确恢复 Warp 窗口的位置。
* 已恢复会话中的命令在本地机器上运行，不再出现在 SSH 服务器的历史记录中。
* 修复了 SSH 到安装了 PackageKit-command-not-found 的 RHEL/Fedora 机器的问题。
* 修复了对 `->` 作为用户提示符的处理不正确的问题。
* 修复了使用 --color 选项时的 ls 补全。

### 2023.01.05 (v0.2023.01.03.08.03)

**Bug 修复**

* 尾随句点不再被视为 URL 的一部分。
* 修复了"自动补全符号"设置未被遵守的回归问题。