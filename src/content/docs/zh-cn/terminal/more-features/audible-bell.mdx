---
title: 可听终端铃声
description: >-
  在 Warp 中启用可由 CLI 工具（如 ping）触发的可听终端铃声。
---

Warp 允许您启用可听终端铃声（默认禁用），可由各种 CLI 工具触发（例如 `ping -a`）。

* 在设置中，在 **设置** > **功能** > **终端** 中启用可听终端铃声。
* 在 [命令面板](/terminal/command-palette/) 中，"启用/禁用可听终端铃声"。